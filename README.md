# MyFirstWebsite 部署与全栈进化实录

## 📅 学习日志

### 2025.12.28
* **初次部署**：理解了 Web 基础架构，掌握了网页如何在浏览器中显示，以及域名如何通过 Cloudflare 指向静态内容。

### 2025.12.29 (今日重点)
* **基础排障**：解决了 `index.html` 链接跳转失败（路径匹配问题）与 `README.md` 格式显示为“一整块”的解析问题。
* **自动化运维实战**：配置 `msmtp` 配合 Resend API，成功在 Ubuntu VPS 上构建了 **SSH 登录自动邮件报警系统**。
* **SMTP 协议深度调试**：攻克了 Resend 报出的 `450 The 'to' field must be a 'string'` 格式错误，深刻理解了 SMTP 协议对邮件头格式的严格要求。
* **性能与视觉优化**：
    * **Favicon 进化**：将 **1024x1024** 的高清原图优化为符合 Web 标准的 **64x64** `favicon.ico`，显著提升加载效率。
    * **3D 背景注入**：引入 **Three.js** 渲染全屏动态粒子系统，为主站增添了科技感视觉深度。
    * **跨域资源共享**：实现了主站与子站 (`pc.yushenchuanmei.top`) 之间的图标资源绝对路径引用。

---

## 🛠️ 原因分析与技术总结

### 1. 路径与命名的“精准打击”
* **问题**：跳转失败多因文件名使用了点号（如 `2025.12.28.1`）、缺失 `.html` 后缀或代码中存在多余空格。
* **经验**：全栈开发中，文件名的大小写、后缀名以及路径字符串的**绝对匹配**是资源加载的首要前提。

### 2. 邮件报警链路的“排坑指南”
* **格式陷阱**：直接使用 `echo` 发送邮件易导致换行符解析失效。
* **对策**：改用 `printf` 严格构造包含 `To:`、`Subject:` 和空行的标准 SMTP 数据包。
* **退信抑制 (Suppression List)**：由于初期未配置 Cloudflare **Catch-all** 导致投递失败，IP 被 Resend 临时拉黑。
* **对策**：开启万能转发规则并手动从 Resend 抑制列表中移除地址，最终打通链路。



### 3. 前端性能与品牌一致性
* **图标规范**：大尺寸图片（1024px）作为 Favicon 会拖慢 FCP（首次内容绘制）。优化为 64px 后，资源请求更轻量。
* **资源架构**：通过 `https://yushenchuanmei.top/favicon.ico` 建立单一事实来源，降低了多仓库间冗余资产的维护成本。

---

## 💡 核心感悟

> **运维是地基**：即便是一个简单的静态页，也需要可靠的监控（如 SSH 报警）来确保环境安全。
> **细节决定成败**：一个多余的 `.png` 后缀或一个缺失的换行符，都可能导致整个系统报错。
> **审美也是战斗力**：Three.js 的加入让单纯的日志变成了具有交互体验的作品。

---

## 🚀 未来计划
* [ ] 在 `pc.yushenchuanmei.top` 完善更多关于 8086 汇编的有符号数逻辑实验。
* [ ] 探索 VPS 端的定时任务 (Crontab)，实现每日运行报告自动推送到邮箱。
* [ ] 进一步优化主站 Three.js 背景在移动端触控时的交互响应。

---
© 2025 YUSHENCHUANMEI // ALL RIGHTS RESERVED
